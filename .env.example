# Prisma Postgres / PostgreSQL (required)
# Prisma Console → your database → Connect → copy connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Optional aliases (Vercel / Prisma integration may inject these)
# POSTGRES_URL=
# PRISMA_DATABASE_URL=

# Required — generate with: openssl rand -base64 32
AUTH_SECRET=

# Canonical app URL (must match the domain in the browser)
# Local:  http://localhost:3000
# Live:   https://www.ichingoracle.de
AUTH_URL=http://localhost:3000

# Google OAuth
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Required for AI interpretations — add to .env.local (Traditional Chinese via gpt-4.1-mini)
# OPENAI_API_KEY=sk-...
# Optional override (default: gpt-4.1-mini)
# OPENAI_MODEL=gpt-4.1-mini

# Future Stripe
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
